1) генерим классы игровых сущностей по entities.js

2) создаем мапу: имя класса -> конструктор + список ассетов

3) на основе файлов уровня генерим наследников ObjectTemplate,
    которые будут создавать игровые объекты из конструкторов,
    подгружать ассеты по необходимости, перегружать св-ва, ставить закон движения..

4) генерим на основе этого всего main:
    отсортированный по posY массив ObjectTemplate,
    генерим и рендерим объекты за X пикселей до их появления,
    они дальше сами крутятся по логике, которая задаётся на этапе 1)

5) компилируем

P.S. надо сделать кэширование классов как-то, чтобы не перегенеривать всё
