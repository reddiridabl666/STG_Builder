#pragma once

#include <SFML/Audio.hpp>
#include <SFML/Graphics.hpp>

#include "Errors.hpp"
#include "Json.hpp"

// class AssetProcessor {
//   public:
//     AssetProcessor(const json::object& asset_map) : asset_map_(asset_map) {}

//     ErrorPtr process_image(const std::string& key, sf::Texture& texture);
//     ErrorPtr process_audio(const std::string& key, sf::Sound& sound);

//   private:
//     json::object asset_map_;
// };
